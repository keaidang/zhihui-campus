# 任务清单

> 更新时间：2026-09-18 21:40 · 状态：全部完成并上线（最新提交 e431651）

## 已完成批次一（上午）

1. ✅ 读取 docs 了解架构
2. ✅ 修复"退课后无法再次选课"（upsert 重激活，cd9bb47）
3. ✅ 选课偶发 500 根治（withTransaction 瞬时重试 + isTransient 扩宽 + 前端 50000 自动重试）
4. ✅ 刷新掉线根治（刷新令牌轮换改原子事务 rotateRefreshTokenAtomic + 前端退避重试）
5. ✅ 全站去角色配色，统一学术深蓝+素金+玻璃拟态，背景实景清晰透出

## 已完成批次二（下午/晚间）

6. ✅ **刷新子路由 404**：新增根目录 edgeone.json 声明 SPA fallback（/* → /index.html，函数与静态资源优先），刷新 /workbench 等路径已返回 200
7. ✅ **学生名单导入导出 + 批量账号管理**：账号管理页支持 CSV 导出（UTF-8 BOM 兼容 Excel）、批量添加（文本粘贴或 CSV 文件，未填密码统一 Zhihui@2026）、批量/单个删除（保护管理员与本人）；数据库 schema-005 迁移已执行
8. ✅ **账号管理与学生管理分离**：账号管理页展示 账号/姓名/权限/学工号/状态/有效期；新增学生管理页（班级维度名册、导入导出、调班）；辅导员数据范围改为"本人带班"（sys_class.counselor_id 绑定，counselor01 带计算机 2601/2602 班）；系部结构 6 学院 7 班级；过期账号登录/刷新被拒
9. ✅ **课表导出**：学生"成绩课表"页、教师"我的课程"页新增导出按钮，生成精美 HTML 课程格（A4 横向打印/存 PDF，课程按颜色区分）
10. ✅ **商用架构调研**：docs/RESEARCH-商用校园平台架构调研.md（正方/青果/强智/寸草心/致远/芒旭格局、"1+1+N"通用架构、对标差距与演进路线）

## 线上验证结论（生产 API 实测）

- 子路由刷新 200 ✅ / 刷新令牌链路 5/5 ✅ / 批量建删+默认密码登录 ✅ / 辅导员仅见本班（计算机 2601/2602）✅ / 班级列表含辅导员刘慧敏 ✅ / CSV 导出 ✅（时间列格式化修复已推送 e431651）
- 选退课瞬时 500 已由约 30% 降至偶发（事务重试+前端兜底），TiDB 免费层网络抖动属平台侧限制

## 已完成批次三（09-19）

11. ✅ **课表导出根治**：
    - 根因一：`section` 字段为 `'1-2节'` 字符串，旧代码 `Number(section)` 提取节次 → 全变 NaN，表格渲染失败
    - 根因二：网格按数据动态生成，无课的整天/整节不显示；根因三：`window.open` 新窗口可能被浏览器拦截
    - 修复：固定网格（周一~周日 × 1-2节~11-12节 六大节次 + 时间提示，无课显示空格，非标准节次自动追加行）；改用页面内浮层 + iframe 预览，提供"打印/另存 PDF"与"关闭"，不再受弹窗拦截影响
12. ✅ **管理员课程与排课**（新页面 /admin/courses，仅 admin 可见）：
    - 课程库：新增/编辑/删除课程（编码唯一，已有教学班的课程禁止删除），课程 CSV 导入（按编码 upsert）与导出
    - 排课管理：为课程指派任课教师 + 学期 + 周几 + 节次 + 教室 + 容量，编辑/删除（已有学生选课禁止删除，可停选），排课 CSV 批量导入（课程编码,教师工号,学期,周几,节次,教室,容量）
    - 新 API：`/api/admin/courses`（GET 列表 + POST 8 类 action，操作全部落 sys_op_log 审计）
13. ✅ **教师查看自己的课**：教师端"我的课程"页 + `/api/edu/timetable`（teacher 角色）已具备，导出复用修复后的固定网格课表
14. ✅ **系部与班级管理补全**（此前只有只读接口、无管理界面）：
    - 新页面 /admin/org「系部与班级」双 Tab（admin 可写，counselor 只读本班）
    - 系部：新增/编辑（名称、排序）/启停/删除（下有人员或班级禁止删除）；`/api/admin/departments` 补 update/delete action
    - 班级：新建/编辑（名称、年级、院系、指派辅导员）/删除（有在读学生禁止删除）；`/api/admin/classes` 补 create/update/delete + 辅导员名单下拉数据
15. ✅ **演示数据批量生成**（scripts/seed-demo.mjs，幂等）：
    - 40 教师 / 8 辅导员（每班已指派）/ 10 校领导（分入各系部）/ 402 学生分入 14 个班级（每班 28~31 人，满足 20~40 人要求）
    - 课程库扩至 27 门、排课 59 个教学班（教师按院系任课、时段教室防冲突）、学生选课约 2000 条（每班本系 3~4 门+公共课，课表有真实数据）
    - 修复脚本批量插入参数错位 bug（占位符 3 个/参数 4 个导致 student_id 串位产生孤儿行），已清理脏数据重跑
    - 生产验证：新账号默认密码登录、teacher02 课表 6 门、student100 课表 5 门、辅导员名册、leader 课表 403 拦截、班级列表人数/辅导员齐全
