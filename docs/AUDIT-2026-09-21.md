# AUDIT · 项目体检报告（2026-09-21）

> 体检范围：线上生产环境 + 全量源码（前端 36 个文件 / 后端 40+ API / 边缘函数 / 构建配置）
> 体检方法：真实库 SQL 一致性核对 + 线上 API 与页面自动化遍历（Playwright + 系统 Edge）+ 依赖审计 + 静态扫描
> 结论：**未发现功能性缺陷**；修复 3 项真实缺陷（安全头缺失、前端无全局错误处理、chunk 失效无兜底）；另有 14 项技术债按 P0~P2 分级列出

---

## 一、本次已修复（3 项）

### 1. 静态资源层完全缺失安全响应头 —— 已修 ✅

**问题**：`node-functions/lib/http.js` 给 **API 响应**加齐了安全头，但 `edgeone.json` 里**没有任何 headers 配置** —— 也就是说用户实际访问的 HTML / JS / CSS（静态层）**一个安全头都没有**。这属于"防护加在了看不见的地方，真正暴露的页面层反而裸奔"。

**修复**（`edgeone.json` 新增 `headers`，EdgeOne 官方支持该字段）：

| 头 | 值 | 作用 |
|---|---|---|
| `Content-Security-Policy` | `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; …` | 防 XSS 注入执行 |
| `Strict-Transport-Security` | `max-age=31536000; includeSubDomains` | 强制 HTTPS，防降级 |
| `X-Frame-Options` | `DENY` | 防点击劫持 |
| `X-Content-Type-Options` | `nosniff` | 防 MIME 嗅探 |
| `Referrer-Policy` | `strict-origin-when-cross-origin` | 收紧 referrer 泄露 |
| `Permissions-Policy` | `geolocation=(), microphone=(), camera=(), payment=()` | 关闭用不到的浏览器能力 |

同时补了缓存策略：`/assets/*` → `max-age=31536000, immutable`（Vite 产物带 hash，可长缓存）；`/index.html` → `no-cache`（否则用户会拿到旧壳）。

**验证**（线上实测 11/11 通过）：6 个安全头全部生效；**CSP 未打断任何功能**——遍历 21 个路由全部正常渲染、**零 CSP 违规、零 JS 运行时错误**。

> ⚠️ 后续维护铁律：改 CSP 必须重跑全站 console 回归（见第五节命令），CSP 是最容易"修了安全问题、顺手搞坏功能"的头。

### 2. 前端缺全局错误处理 —— 已修 ✅

**问题**：`src/main.js` 从未配置 `app.config.errorHandler`。Vue 的默认行为是：**未捕获的组件异常会中断该组件树的渲染**，用户看到的是空白或半截页面，且控制台之外没有任何兜底。项目此前多处"页面空白"现象与此机制有关。

**修复**：`main.js` 补 `app.config.errorHandler` + `window.addEventListener('unhandledrejection')`，统一记录 `[vue-error]` / `[unhandled-rejection]`。**刻意不弹窗**——业务错误已由 `api/request.js` 统一提示，重复弹窗只会打扰用户。

### 3. 懒加载 chunk 失效无兜底（部署后白屏）—— 已修 ✅

**问题**：本项目 26 个路由中 22 个走 `() => import()` 懒加载，且**部署非常频繁**（改一行 CSS 也会重新构建、产物 hash 全变）。典型故障场景：用户停在旧页面 → 新版本部署完成 → 用户点击进入新路由 → 浏览器去请求**已被替换的旧 chunk** → 404 → 动态 import 抛错 → **整页白屏**。此前 `router` 里没有 `onError`，这个场景没有任何兜底。

**修复**（`src/router/index.js`）：`router.onError` 捕获 `Failed to fetch dynamically imported module` 等错误 → 自动硬刷新一次拿最新 `index.html`；用 `chunkReloaded` 标志位防止刷新循环。

---

## 二、排查后确认健康的项（无需整改）

| 维度 | 检查内容 | 结果 |
|---|---|---|
| **数据一致性** | 18 类核对：孤儿住宿/角色/选课/站内信/帖子/借阅记录、重复在住、房间占用数与实际入住数、图书可用数与在借数、请假与报修缺流程实例、审批流实例缺节点、学生缺性别、住宿与楼栋性别不匹配、重复邮箱、无角色用户 | **全部 0 不一致** |
| **SQL 注入** | 全量扫描 `query(\`…\`)` 模板插值点 | 全部为参数化占位符；动态 `SET` 子句的**字段名硬编码白名单**、值走 `?`；`IN (${ph})` 为占位符数组 —— **无注入面** |
| **鉴权覆盖** | 所有含 POST 写的端点 | **100% 有 `requireAuth`/`requireRoles`**；路由层 26 条中 22 条 `requiresAuth`、12 条带 `roles` 限制 |
| **依赖安全** | `npm audit --omit=dev` | **0 vulnerabilities** |
| **移动端适配** | 22 个路由 × 390×844 视口，逐个测 `documentElement.scrollWidth - innerWidth` | **22/22 零横向溢出**（表格内部横向滚动属预期行为） |
| **线上运行错误** | `sys_op_log` 按 detail 分组、看 `MAX(created_at)` | 最近一条为 136 分钟前的**历史噪音**（M4 验证期已修 bug 的留痕），**无新增** |
| **代码卫生** | TODO/FIXME/HACK、遗留 `console.log`、`<script setup>` 里 ref 漏 `.value`（模板字符串场景） | **全部 0** |

---

## 三、遗留缺陷与整改建议（按优先级）

### P0 · 对外开放前必须处理

**P0-1 演示账号弱密码 `admin/admin`，且写在文档与仓库里**
- 风险：任何拿到仓库或文档的人可直取超管权限。当前 `docs/HANDOVER.md` 第 2 节明列账号密码。
- 建议：① 正式对外前改为强密码并**从文档中移除明文**（换成"见部署方保管"）；② 演示用账号单独建低权限账号，与超管解耦；③ 文档只保留账号名与"密码由部署方设置"。
- 工作量：10 分钟（改密码 + 改 3 处文档）。

### P1 · 影响可维护性与演进

**P1-1 零自动化测试、零 lint/typecheck**（本次体检最大工程债）
- 现状：无 `test/` 目录、无 ESLint/Prettier 配置、`package.json` 无 `test`/`lint` 脚本。回归完全依赖人工写一次性 Node 脚本（每次几十行、跑完即删，知识不沉淀）。
- 风险：本项目已有 11 个 schema、40+ API、5 类角色权限矩阵，任何改动都靠"人肉回归"，越往后越危险；本次体检发现的 3 个缺陷本来都可以被测试提前拦住。
- 建议（渐进，不必一步到位）：
  1. 先把已有的**冒烟脚本固化**：`scripts/smoke-auth.mjs`、`sql-smoke-m4.mjs`、`gap-check.mjs`、`integrity-check.mjs` 归到 `npm run check` 一条命令；
  2. 引入 ESLint（`eslint-plugin-vue` + `no-unused-vars` 即可，不追求全规则）并进构建前检查；
  3. 关键路径写 Vitest 单测：权限判定（`guard.js` 三层防线）、选课防超卖条件更新、宿舍性别约束、时间口径 `fmtTime`；
  4. 长期：把"线上 21 步冒烟"沉淀为可重复的 Playwright 脚本（`scripts/e2e-smoke.mjs`），取代每次临时手写。
- 工作量：步骤 1 约 1 小时；步骤 2 约 2 小时；步骤 3 约 4~6 小时。

**P1-2 报修业务存在两条写入路径，数据模型不一致**
- 现状：`/api/af/repair`（M2 老接口）提交时会建 `flow_instance`；`/api/dorm/repair`（M4 并入宿舍管理后）**直接写 `af_repair`、不建流程实例**。实测库中 2 条报修无流程实例（`#1` 09-17 老数据、`#2000001` 09-20 M4 后提交）。
- 影响：`flow_instance/flow_node` 这套"通用审批流引擎"是论文核心技术点之一，但报修线数据不完整 —— 答辩若被追问"审批流覆盖哪些业务"，口径会含糊。
- 建议：二选一并统一——① **报修只走工单状态机、明确定义为"非审批型业务"**，并在 DATABASE.md 写清"通用审批流适用于请销假/社团等需多节点审批的业务，报修是单节点受理工单"（推荐，改动小、口径清晰）；② 或让 `/api/dorm/repair` 也建流程实例（改动中等，需迁移存量 2 条）。
- 工作量：方案 ① 仅文档，20 分钟。

**P1-3 Element Plus 全量引入，单 chunk 1.09 MB** —— ✅ **已于 2026-09-21 修复**（改为按需引入后：最大 chunk 1088KB→172KB、gzip 341KB→~145KB、首页首屏 JS 最大 62KB；做法与三个易漏点见 HANDOVER 铁律 #33）
- 现状：`element-plus` 全量注册（`app.use(ElementPlus)` + 全局注册所有图标），构建产物 EP 单包 1.09 MB（gzip 341 KB）。已做 manualChunks 分包（主包仅 18.7 KB），但首屏仍要拉整个 EP。
- 建议：引入 `unplugin-vue-components` + `unplugin-auto-import` 做按需引入，预计 EP 体积可降 50%~70%。属**优化项**，不影响功能，建议放在论文定稿后（回归成本高于收益）。
- 工作量：2~3 小时（含全站视觉回归）。

**P1-4 图片仍以 LONGBLOB 存在业务库里**
- 现状：`sys_blob` 表 32 条 / 1.15 MB，图片二进制直接进 TiDB，`/api/blob?token=` 读出。
- 风险：随图片增长，数据库体积、备份时间、查询性能都会受影响；TiDB Serverless 免费额度有限。
- 建议：接对象存储/图床，**只需改 `blob.js` 与 `ImgUploader` 的 URL 生成**（schema 与调用方都不用动——当初已预留这个切面）。属中期项。
- 工作量：2~4 小时。

**P1-5 校园邮箱密码明文入库 + 明文导出且无审计**（2026-09-21 追加，实测核实）
- 背景核实：**登录口令不是明文**——`sys_user.password_hash` 为 bcrypt（实测 490 条全部 `$2a$10$…`、60 字符，0 条非 bcrypt）；刷新令牌存 `SHA-256`（实测 64 字符齐全）；邮箱验证码有 10 分钟时效（`expires_at < NOW()` 在 SQL 侧判定，`register.js:65` / `forgot-reset.js:42`）。这三处无问题。
- 问题：`sys_user.mail_password VARCHAR(64)` 存**明文**，代码中无任何加解密（全库 grep `encrypt|decrypt|cipher|aes` 为空）。实测 2 条已开通邮箱的记录，库里就是可读明文（如 `admin@keaidang.com` 对应的一串 12 位密码）。
- **这是刻意的设计**（schema 注释自述"管理员可见可导出"），因为它是第三方邮箱服务的凭据，业务上需要在用户忘记时能取回。但也意味着：
  - `GET /api/admin/mailbox?export=csv` 会把**全站邮箱密码明文**导成 CSV（`mailbox.js:40`）；
  - `UserManageView` 有「密码」按钮直接弹窗展示明文（`:393`）；
  - 用户可自行改自己的邮箱密码（`/api/me/mail-password`）。
- **附带发现（独立缺陷）**：`mailbox.js` 的 4 个写操作（enable/disable/resetPassword/updateAddress）都调了 `opLog` 留痕，但 **GET 分支（含明文 CSV 导出）在 `mailbox.js:34-50` 直接 return，没有任何 `opLog`** —— `requireRoles` 自身也不写日志（`guard.js:48-64` 仅做校验）。即"一键导出全校邮箱明文密码"这个最敏感的动作**零审计留痕**。
- 风险量化：泄露后攻击者可登录用户校园邮箱并以学校名义发信；且导出动作无法追溯是谁导的。属"内部人员误用/越权不可追溯"类风险。
- 建议（按性价比）：
  1. **【必做·10 分钟】给明文导出与查看加 `opLog`**（`mailbox.export` / `mailbox.viewPassword`），动作可追溯。零风险、无 schema 变更、无需新增密钥。
  2. **【推荐】改为 AES-256-GCM 可逆加密存储**：密钥放环境变量，读写两侧加解密。管理员仍能查看（业务不变），但库被拖走时不是明文，且能发现是否被人篡改。改动约 30 行 + 4 个读写点 + 迁移存量 2 行。**需要先确认密钥保管方式**（丢失则邮箱密码不可恢复，只损失"查看"能力不影响用户登录邮件系统）。
  3. 不可选项：改不可逆哈希——业务上必须能取回展示，不可行。
- 工作量：方案 1 约 10 分钟；方案 2 约 1 小时（含密钥落地与存量迁移）。

### P2 · 优化与细节

| 编号 | 项 | 说明与建议 | 工作量 |
|---|---|---|---|
| P2-1 | 图书封面 420 本全空 | `cover_url` 全为空串，前端显示首字占位。可按分类批量补 picsum 图或生成统一占位图 | 1 小时 |
| P2-2 | 邮箱附件发信 / 用量统计未做 | LanQin API 支持 attachment，属功能补全 | 3~4 小时 |
| P2-3 | 学期口径硬编码 | `TERM='2026-2027-1'` 散落在 edu 相关 API，换学期需全局改。建议提取为环境变量或 `sys_config` 表 | 1 小时 |
| P2-4 | 无结构化日志与告警 | 错误只落 `sys_op_log`，无主动告警（线上出错要人去翻表）。建议加：错误率超阈值时通过校园邮箱给自己发信 | 2 小时 |
| P2-5 | 无备份/恢复演练记录 | TiDB 免费版有自动备份，但**从未验证过恢复流程**。建议做一次"导出 → 建临时库 → 导入 → 校验行数"的书面演练 | 1 小时 |
| P2-6 | 残留测试账号 `zhreg2871` | id 12209012，注册测试用，确认无用后删除 | 5 分钟 |
| P2-7 | App 壳未打包 | 移动端 H5 已就绪，APK 需云打包（本机无 Java/Android SDK） | 1~2 小时 |
| P2-8 | 过期数据清理未定时执行 | `cleanup.mjs` 已就绪但靠手动跑（本次 dry-run 为 0 行，因为数据都很新）。建议配成每月一次的自动化任务 | 20 分钟 |
| P2-9 | API 层与静态层安全头双份维护 | API 头在 `http.js`、静态头在 `edgeone.json`，两处需同步。建议在 CONVENTIONS.md 注明"新增安全头需同时改两处" | 10 分钟 |

---

## 四、结论

- **功能与数据是健康的**：没有孤儿数据、没有状态不一致、没有注入面、没有鉴权漏洞、没有依赖漏洞、移动端 22 个页面零溢出、线上零新增错误。
- **真正缺的是"工程护栏"**：无测试、无 lint、无监控告警 —— 这次的 3 个缺陷全都属于"机制缺失"而非"写错了代码"。系统已经大到只靠人工冒烟不足以支撑继续演进。
- **优先级建议**：P0-1（改演示密码）→ P1-1 步骤 1（把已有冒烟脚本固化成一条 `npm run check`）→ P1-2（统一报修口径，纯文档）→ 其余按论文进度穿插。

---

## 五、复现命令（本次体检全部可重跑）

```bash
cd /c/Users/Administrator/Desktop/zhihui-campus

# 1. 规模/角色/剩余项/账号残留/线上错误最后发生时间
node scripts/gap-check.mjs

# 2. 数据一致性 18 类核对（只读）
node scripts/integrity-check.mjs

# 3. 过期数据清理预演（默认 dry-run，--yes 才删）
node scripts/cleanup.mjs

# 4. 依赖漏洞
npm audit --omit=dev

# 5. 移动端全站溢出 + 线上安全头/CSP 回归（Playwright + 系统 Edge，脚本在 .shots/）
node .shots/audit-mobile.mjs      # 22 路由 × 手机视口溢出
node .shots/verify-security.mjs   # 安全头 + CSP 全站 console 回归
node .shots/verify-live.mjs       # 移动端适配 + PC 零改动 17 项断言
```

> `.shots/` 为本地验收产物（已 gitignore）。Playwright 依赖装在托管工作区
> `~/.workbuddy/binaries/node/workspace`，用系统 Edge（`channel:'msedge'`）零下载启动。
