# 机密与本地配置
.env
.env.local
*.pem.bak

# scripts 目录白名单制：只保留正式脚本，其余为本地临时产物
scripts/*
!scripts/db-test.py
!scripts/run-schema.py
!scripts/smoke-auth.mjs
!scripts/eo.cmd
!scripts/migrate.mjs
!scripts/grant-role.mjs
!scripts/seed-demo.mjs
!scripts/seed-admin-staff.mjs
!scripts/seed-m3.mjs
!scripts/seed-dorm.mjs
!scripts/sql-smoke-m4.mjs
!scripts/gap-check.mjs
!scripts/integrity-check.mjs
!scripts/cleanup.mjs
!scripts/ops-check.mjs
!scripts/make-readme-shots.mjs

# 调试探针与本地验证产物（严禁入库）
probe*
canary*
*-report.txt
*-check.txt
last*.txt
push-result.txt
live-check.txt
health-live.txt
deploy-check.txt
verify.txt
b.txt
c.txt
tracked.txt

# 根目录 / node-functions 下的临时验证产物（全为本地调试用，一律不入库）
/*.txt
node-functions/*.txt

# Node/构建日志
*.log
npm-exit.txt
npm-build-exit.txt
npm-build2-exit.txt

# 依赖与构建产物
node_modules/
dist/
build/
.unpackage/

# 系统文件
.DS_Store
Thumbs.db
audit-status.txt
s.txt
node-ok.txt

# 手动下载的仓库副本与一次性验证脚本（严禁入库）
zhihui-campus-main/
check-*.mjs
verify-*.mjs

# 会话/调试工作目录（严禁入库）
working/

# 线上端到端冒烟（多角色 + 越权边界 + 历史缺陷回归，只读可重复运行）
# 已取代旧的 smoke-full.mjs（那个脚本硬编码了已失效的域名 campus.keaidang.com）
!scripts/e2e-smoke.mjs

# 移动端验收截图（本地产物，不入库）
.shots/

# 毕业设计论文交付物 —— 含真实姓名/学号/学校等个人身份信息，**严禁入库**
# （2026-09-22 已从全部 git 历史中移除；文件本身保留在本地 docs/ 下）
docs/论文初稿.*

# README 完整性自检脚本
# ⚠ 必须放在上面 `check-*.mjs` 规则**之后** —— gitignore 后匹配的规则优先，
#   放在前面会被 check-*.mjs 覆盖掉（实测踩过）
!scripts/check-readme.mjs
